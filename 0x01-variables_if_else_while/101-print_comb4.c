#include <stdio.h>

/**
 * main - print all combination of three different digits in ascending order, se *	  parated by a comma
 *
 * Return: Always 0.
 */
int main(void)
{
	int digit1, digit2, digit3;

	for (digit1 = 0; digit1 < 8; digit++)
