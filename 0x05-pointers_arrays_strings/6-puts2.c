#include "main.h"

/**
 * puts2 - prints one char out of 2 of a string
 * @str: the string containing characters.
 */
void puts2(char *str)
{
	char *str;

	str = "0123456789";
	puts(str);
	return (0);
}
